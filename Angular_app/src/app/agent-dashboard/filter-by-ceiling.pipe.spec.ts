import { FilterByCeilingPipe } from './filter-by-ceiling.pipe';

describe('FilterByCeilingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCeilingPipe();
    expect(pipe).toBeTruthy();
  });
});
